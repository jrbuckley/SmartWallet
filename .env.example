# Postgres connection string.
# Week 2 wires this up (local Postgres, Neon, Supabase, or RDS).
# Until then the app builds and runs on seed data in lib/data.ts.
DATABASE_URL="postgresql://user:password@localhost:5432/smartwallet"
